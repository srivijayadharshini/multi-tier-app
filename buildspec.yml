version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18

  build:
    commands:
      - echo "Build Started"
      - echo "Deploying Student Result Management System"

artifacts:
  files:
    - '**/*'
